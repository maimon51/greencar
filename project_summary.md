# סיכום פרויקט Greencar - ארכיטקטורה, נתונים ופיתוח

מסמך זה מסכם את עבודת הפיתוח, ארכיטקטורת הנתונים, מקורות המידע והממשק שנבנו במסגרת פרויקט Greencar.

## 📊 1. מקורות המידע (Data Sources)
הפרויקט נשען על אינטגרציה רציפה ומתוחכמת של מספר מאגרי מידע ממשלתיים (Data.gov.il) יחד עם העשרה חיצונית מבוססת בינה מלאכותית:
1. **מאגר רכבים מורשים (Car Models API):** בסיס הנתונים הראשוני המספק את כלל דגמי הרכב שהורשו ליבוא לישראל, כולל מפרטים טכניים, רמות בטיחות, וזיהום אוויר.
2. **מאגר קריאות שירות (Recalls API):** מאגר ממשלתי המסנכרן קריאות שירות בטיחותיות של יצרנים, המקושרות בזמן אמת לדגמים הרלוונטיים.
3. **מחירי יבואן - רכב חדש (MSRP API):** מאגר המכיל את מחיר המחירון הרשמי בעת שחרור הרכב (MSRP).
4. **מאגר רכב מנועי פעיל (Active Vehicles API):** מאגר ענק של כ-3.5 מיליון רשומות. פיתחנו סקריפט Map-Reduce ייעודי העוקף מגבלות חומת אש (WAF) על ידי ביצוע Pagination של 32,000 רשומות בכל קריאה, סכימה של הרכבים בזיכרון השרת, ועדכון מסד הנתונים בצורה מבוזרת כדי לא להעמיס על השרת.
5. **מנוע העשרה מבוסס בינה מלאכותית (LLM Worker):** מנגנון רקע שקורא דגמים חסרים ממסד הנתונים ופונה למודלים מתקדמים כדי למשוך: שמות מסחריים (למשל "קורולה" במקום קוד טכני), קישורים לתמונות איכותיות, מפרטי האצה (0-100), ציון אמינות ורשימת תקלות נפוצות מהרשת.

---

## 🗄️ 2. סכמת מסד הנתונים (Database Schema)
ארכיטקטורת מסד הנתונים (PostgreSQL + Prisma) נבנתה בצורה חכמה ומדורגת כדי לאפשר שאילתות סטטיסטיות מהירות (Analytics):

* **`Manufacturer` (יצרן):**
  * שומר את קוד היצרן (למשל משרד התחבורה: `tozeret_cd`), שם היצרן, וארץ הייצור.
* **`CarModel` (דגם רכב - "מטרייה"):**
  * מייצג את שם הדגם הכללי (לדוגמה "יונדאי טוסון").
  * **שדות בולטים:** קוד דגם (`code`), שם מסחרי (`commercialName`), תמונה (`imageUrl`), ציון אמינות (`reliabilityScore`), ומערך של תקלות נפוצות (`knownIssues`). בנוסף ישנו שדה מפרט מורחב בפורמט JSON (`extraSpecs`).
* **`TrimLevel` (רמת גימור ושנתון) - "לב המערכת":**
  * זוהי הרמה הגרנולרית (המפורטת) ביותר. לכל דגם רכב יכולות להיות עשרות רמות גימור על פני שנות ייצור שונות.
  * **שדות טכניים:** נפח מנוע, כוח סוס, תיבת הילוכים, פליטת CO2, מדד ירוק (1-15).
  * **בטיחות:** שדות בוליאניים נפרדים לכל מערכת בטיחות (בקרת סטייה מנתיב, שטח מת, בלימת חירום וכו') וציון בטיחות משוקלל.
  * **פיננסי וסטטיסטי:** `msrp` (מחיר רשמי ליבואן) ו-`activeCount` (כמות הרכבים הספציפיים מרמת הגימור הזו שנעים כיום על הכביש בישראל).
* **`Recall` (קריאת שירות):**
  * שומר תאור התקלה (`description`) ותאריך (`date`), עם קשרי גומלין ליצרן ולדגם הרכב.

> **💡 היתרון בארכיטקטורה:** מיקום השדה `activeCount` ישירות בטבלת ה-`TrimLevel` מאפשר לנו לבצע פעולות אגרגציה (`SUM`) מהירות ברמת השנה, ברמת הדגם הכולל, או ברמת היצרן - מבלי לשמור 3.5 מיליון רשומות במסד הנתונים (מה ששומר על עלויות אחסון אפסיות ומאפשר עבודה חלקה ומהירה בייצור).

---

## 🖥️ 3. פיתוח חזית וממשק משתמש (Frontend & UI/UX)
האתר נבנה בטכנולוגיית Next.js (App Router) ועבר שדרוג משמעותי לעיצוב יוקרתי ואפל (Dark Mode Glassmorphism) כדי להציג את כמויות המידע האדירות בצורה פשוטה, שיווקית ומרשימה:

1. **דף הבית (Home Page):**
   * **חיפוש מהיר (Quick Search):** תפריטים נפתחים (Dropdowns) מותנים של יצרן -> דגם כדי לנווט במהירות, מבוססי שמות מסחריים נקיים ללא כפילויות.
   * **דירוגי פופולריות דינמיים:** רשימת ה"יצרנים הפופולריים" וה"דגמים הנבחרים" נמשכת בזמן אמת ממסד הנתונים ומדורגת על בסיס **כמות הרכבים הפעילים בפועל על הכביש** (`activeCount`), עם תצוגת מספרים מדוייקת.
2. **דף דגם רכב מפורט (`/cars/[brandId]/[modelId]`):**
   * תצוגת גיבור (Hero) מרהיבה עם שם היצרן, הדגם, ותמונת הרכב.
   * **תג פופולריות 🏆:** במידה וישנם רכבים על הכביש, מוצגת בועה זוהרת המציינת כמה רכבים פעילים יש לדגם זה על כבישי ישראל.
   * מפרטים טכניים מלאים, ציון בטיחות מ-0 עד 8, מדד זיהום אוויר, ציון אמינות (מ-1 עד 100) ותקלות שכיחות.
   * כרטיסיות (Tabs) לפי שנת ייצור המאפשרות לדפדף בקלות בין עשרות רמות הגימור. לכל רמת גימור נוספה בועית עם מחיר המחירון הישן (MSRP).
   * אזור קריאות שירות (Recalls) מודגש באדום לקריאות בטיחות.
3. **דשבורד נתוני שוק חכמים (`/insights`):**
   * עמוד אנליטיקה ציבורי חדש בעל קישור מודגש בסרגל הניווט הראשי. העמוד כולל:
     * מספר הרכבים הפעילים הכולל בישראל.
     * טבלת Top 20 של היצרנים הגדולים בישראל לפי כמות רכבים על הכביש.
     * טבלת Top 20 של דגמי הרכב הספציפיים הנפוצים ביותר (משולב עם תמונות ה-AI של הרכבים).
4. **אינטגרציית "Green Car" שיווקית:**
   * הוטמע הלוגו הרשמי של סוכנות Green Car בתפריט העליון ובמסכי הטעינה.
   * שולבו אזורי הנעה לפעולה (CTA) עשירים בדף הבית ובעמודי הדגמים, המפנים את הגולש ישירות לבדיקת מלאי באתר הרשמי (`green-car.co.il`), או לחיוג מיידי למוקד (*8523). עיצוב האלמנטים השיווקיים כולל צלליות ואנימציות מודרניות.
